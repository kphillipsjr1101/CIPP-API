interface:
  display_name: "CIPP API"
  short_description: "Repo-specific patterns and workflows for CIPP-API"
  default_prompt: "Use the CIPP-API repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true